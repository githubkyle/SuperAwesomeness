<h1>The Tech Blog</h1>
{{#each blogs}}
<section class="card">
  <a href="/blogs/{{note.id}}" class="card-body">
    <h3 class="card-title">{{note.title}}</h3>
    <h5>Posted by {{user.username}} on {{createdAt}} with the tag of {{note.tag}}</h5>
    <p class="card-text">{{note.text}}</p>
  </a>
</section>
{{/each}}